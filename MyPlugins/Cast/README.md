A plug-in that allows you to cast the current playing song. Definitely not recommended to install (yet), because it is really buggy and not well developed.

If you want to test it, run `npm install` first.